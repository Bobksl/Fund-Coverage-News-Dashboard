# Dashboard release — 24 September 2026

Integrates Codex pipeline and Claude UI/report work from codex/news-events-priority. User authorized public publication, including both reports. Existing main baseline is b0edb36; fetched before integration. No raw date file, index or status changes in this release. The user's untracked .claude/ is excluded.

## Release review

- Claude handback at 514897c independently reproduced: 396 tests and 14 subtests passed, including 15 real Chromium UI tests; no skips.
- Report SHA-256 values independently match handback: English 84e9a89bb7f27dc6f1cb316b810dcecb46d6ab89f7799e71bc9b725e15a0ab0c; Chinese 4f8e456ab7dd821a0cc7283bd38aceb3b334dfacd1936d3f6edae96cccf5d106.
- Additional review found a stale date view could omit the second loaded member of an event. New Chromium regression reproduced the failure. Membership is now checked for every loaded card before skipping an already-checked event.
- Archive partition independently checked: 34 dates, 182 articles, 174 events, eight pairs, all source IDs preserved exactly once.
- Changed Python files pass Ruff; git diff --check passes.
- Final full suite after the fix: 397 passed, 14 subtests passed, including 16 Chromium tests; no skips (58.62 seconds).

## Publication and follow-up

Release uses an ordinary fast-forward push to main, preserving scheduled data; no force push. The existing push workflow deploys public/ and skips refresh, so this release does not itself run a paid model call.

All legacy events remain Needs review. Historical evidence recovery, independent semantic review and analyst ranking acceptance are still open. Synthetic priority tests do not establish editorial accuracy or live model performance. The previously failed calibration has not been reclassified as passed.

English and approved Chinese v2 are the report versions included. Chinese chart labels, Exhibit 9 note and footer remain as approved; further analyst corrections belong in v3.

Rollback: revert release code/UI commits on current main without resetting history or removing newer daily data. Regenerate events from retained source files if needed, run checks, and push normally to redeploy. Reports can be independently reverted or replaced.

## Deployment evidence

- Release commit c616729c6dd2fac941c74ab180f135e4214bfddd pushed normally to main, advancing remote b0edb36. No force push and no newer remote data to merge.
- GitHub Actions run https://github.com/Bobksl/Fund-Coverage-News-Dashboard/actions/runs/35954519275 completed successfully: deploy succeeded, refresh skipped.
- Live app.js, events.json and both PDFs return HTTP 200 and SHA-256 matches to committed Git bytes. Text files were compared to Git blobs to avoid Windows checkout CRLF differences. PDFs are served as application/pdf.
- Live browser All dates reads 174 events / 182 articles / 34 dates, with eight Sources (2) disclosures. Reports language selection switches Open/Download links between approved English v2 and Chinese v2.
- Native PDF embedding remains UNVERIFIED. The connected browser inventory exposes only Codex in-app browser; Chrome is unavailable. Both embedded panes were blank there. A desktop Chrome/Edge PDF-viewer check remains required; successful HTTP responses and object URLs do not prove rendered pages. Open/Download links remain visible.

## Concurrent refresh preserved

While publishing documentation, remote main advanced to ce904a6 (Refresh news data). The normal push rejected the race; fetched and merged without overwriting data. Refresh/deploy run https://github.com/Bobksl/Fund-Coverage-News-Dashboard/actions/runs/35954603491 succeeded. Its status reports 28 feeds successful, 19 added and 11 rejected by the model. This provides live refresh execution evidence, not editorial accuracy acceptance.

The new archive contains 35 dates, 201 articles and 193 events, all Needs review. Source-member partition passes exactly. Live browser confirms these totals and eight Sources (2) groups. The original 174/182 checks above refer to the release snapshot before this refresh.

The refresh exposed fixed archive totals in browser tests (observed failure: 193 != 174). Assertions now derive totals from the current archive while still checking exact representative order, source preservation, filters and fallback behavior. Final full suite on the merged refreshed archive: 397 passed, 14 subtests passed, no skips (27.67 seconds); Ruff and diff checks pass. No production data was modified by tests.
